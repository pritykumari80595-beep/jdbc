# jdbc
This Java JDBC program is a console-based hotel guest management system. It connects to a MySQL database and allows users to insert, update, delete, and retrieve guest records using a menu-driven interface. PreparedStatements are used for secure database operations.
